/**
 * Deliberately a generic brand tagline rather than a specific, unverified
 * policy claim (e.g. a dollar-amount shipping threshold) — swap this copy
 * for a real, confirmed shipping/promo message whenever one exists.
 */
const AnnouncementBar = () => {
  return (
    <div className="w-full bg-forest text-paper text-center py-2 txt-compact-small">
      Thoughtfully made goods for dogs and cats, picked with care.
    </div>
  )
}

export default AnnouncementBar
